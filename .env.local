# Email Configuration for Nodemailer
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=support@mira.dz

# For Gmail accounts, you'll need to create an "App Password"
# Go to your Google Account > Security > 2-Step Verification > App passwords
# Create a new app password and paste it in EMAIL_PASS

# Agency email
AGENCY_EMAIL=sitekdigital@gmail.com

# For testing without sending real emails, simply leave EMAIL_USER and EMAIL_PASS empty
# The system will log emails to the console instead 